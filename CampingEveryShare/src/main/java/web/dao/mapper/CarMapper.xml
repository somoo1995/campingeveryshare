<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="web.dao.face.CarDao">
	
	<sql id="selectCar">
		SELECT car_no, user_id, car_name, car_number, location, area, area_detail, car_size, car_pax
		, car_spax, price, car_elec, car_water, car_table, car_counter, car_kitchin, car_temp, car_shower
		, car_toilet, car_pet, car_smoke, car_status, content, delete_status
		FROM cartb
	</sql>

	<select id="selectCntAll" resultType="int" parameterType="web.util.Paging">
		SELECT count(*) FROM cartb
		<where>
			AND car_status = 1
			AND delete_status = 0
		</where>
	</select>
	
	<select id="selectAll" parameterType="web.util.Paging" resultType="Car">
		SELECT * FROM (
		SELECT rownum rnum, R.* FROM (
			<include refid="selectCar" />
			<where>
				AND delete_status = 0
			</where>
			ORDER BY car_no DESC
		) R )
		<where>
			AND rnum BETWEEN #{startNo } AND #{endNo }
		</where>
	</select>
	
	<select id="selectByCarNo" parameterType="Car" resultType="Car">
		<include refid="selectCar" />
		<where>
			AND car_no = #{carNo }
		</where>
	</select>

</mapper>