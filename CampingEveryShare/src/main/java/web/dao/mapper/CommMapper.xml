<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="web.dao.face.CommDao">
  	<select id="selectComm" resultType="comm" parameterType="comm">
		SELECT * FROM (
			SELECT rownum rnum, B.* FROM (
				SELECT
					comm_no
					, board_no
					, user_id
					, content
					, post_date
					, delete_status
					, board_cate
					, (SELECT user_nick FROM usertb WHERE user_id = commtb.user_id) AS user_nick
				FROM commTb
				WHERE board_no = #{boardNo }
				ORDER BY post_date DESC
			) B
		)
		ORDER BY rnum
  	</select>
  	 
  	<insert id="insertCommByShare" parameterType="comm">
		INSERT INTO commTB ( comm_no, board_no, user_id, content, board_cate )
		VALUES ( comm_seq.nextval, #{boardNo}, #{userId}, #{content}, #{boardCate} )
  	</insert>
  
<!-- 	<select id="selectCntRecommend" resultType="int" parameterType="comm"> -->
<!-- 		SELECT count(*) FROM recommend -->
<!-- 	<where> -->
<!-- 		AND user_id = #{userId} -->
<!-- 		AND board_no = #{boardNo} -->
<!-- 	</where> -->
<!-- 	</select> -->
  </mapper>