<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="web.dao.face.ShareDao">
  	<select id="selectShareAll" parameterType="web.util.Paging" resultType="Map">
  		SELECT * FROM (
	  	SELECT ROWNUM rnum, B.* FROM (
			SELECT board_no
			, title
			, location
			, post_date
			, hit 
			, content
			, delete_status
			, board_cate
			, (SELECT cate_name_en FROM board_category WHERE board_cate = 2) AS "cate_name_en" 
			, (SELECT cate_name_ko FROM board_category WHERE board_cate = 2) AS "cate_name_ko" 
			, (SELECT COUNT(*) FROM recomtb WHERE board_no = boardtb.board_no) AS "recommend"
			, boardtb.user_id
			, user_nick
			, user_name
			FROM boardtb
			JOIN usertb ON boardtb.user_id = usertb.user_id
			ORDER BY board_no DESC
	    ) B
	) WHERE rnum BETWEEN #{startNo} AND #{endNo}
		AND board_cate = 2
  	</select>
  
  	<select id="selectCntAll">
  		SELECT count(*) FROM boardtb
  	</select>
  
  	<select id="selectBoardView" resultType="boardtb" parameterType="boardtb">
	SELECT
	    board_no,
	    title,
	    location,
	    post_date,
	    hit,
	    content,
	    delete_status,
	    board_cate,
	    user_id
	FROM boardtb
	WHERE board_no = #{boardNo }
  	</select>
  	
  	<select id="selectByUserNick" resultType="usertb" parameterType="usertb">
  	SELECT user_nick
  	FROM usertb
  	WHERE user_id = #{userId }
  	</select>
  	
  	<update id="hit" parameterType="boardtb">
  		UPDATE boardtb
  		SET hit = hit+1
  		WHERE board_no = #{boardNo}
  	</update>

	<select id="selectGetFileByBoardNo" resultType="boardfiletb" parameterType="boardfiletb">
		SELECT file_no, origin_name, stored_name, board_no, board_cate
		FROM boardfiletb
		WHERE board_no = #{boardNo}
	</select>
	
  	<insert id="insertShareWrite" parameterType="boardtb">
		<selectKey order="BEFORE"  resultType="int" keyProperty="boardNo">
			SELECT board_seq.nextval FROM dual
		</selectKey> 
  		INSERT INTO boardtb (board_no, title,content, user_id, board_cate, location)
  		VALUES (#{boardNo}, #{title}, #{content}, #{userId}, #{boardCate}, #{location} )
  	</insert>

	<insert id="insertShareFile" parameterType="boardfiletb">
		<selectKey order="BEFORE" resultType="int" keyProperty="fileNo">
			SELECT file_seq.nextval FROM dual
		</selectKey>
		INSERT INTO boardfiletb ( file_no, origin_name, stored_name , board_no, board_cate)
		VALUES ( #{fileNo}, #{originName}, #{storedName}, #{boardNo}, #{boardCate})
	</insert>  
  
  </mapper>